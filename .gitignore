# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase credentials — never commit
.env
.env.*
!.env.example

# Interne Übergabe-Notizen: offene Schwachstellen, Zuständigkeiten, Betriebswissen.
# Claude Code lädt die Datei automatisch mit, sie darf aber nicht ins öffentliche
# Repo — siehe CLAUDE.md.
CLAUDE.local.md

# Datenschutz-Unterlagen: enthalten Bewertungen offener Risiken und interne
# Namen. Gehören zum DPO/Legal, nicht in ein öffentliches Repository.
DSGVO-Paket.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
